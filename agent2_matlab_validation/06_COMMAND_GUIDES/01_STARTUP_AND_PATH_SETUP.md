# Startup and Path Setup

Run these MATLAB commands from the project root:

```matlab
cd('C:\Users\MSI\Desktop\2025 UH\Matlab')
startup
pwd
path
```

Check that MATLAB can see the project scripts:

```matlab
which startup
which phase2a_patch_validation
which test_db_connection
```

If `startup` adds the expected paths, the project should show the `config`, `lib`, and `scripts` folders in the MATLAB path.
