# MATLAB DB Troubleshooting

- JDBC connection fail
- wrong PostgreSQL host/IP
- PostgreSQL service not running in VMware
- Windows cannot access Linux path
- Database Toolbox missing
- JDBC driver missing
- wrong password
- case-sensitive Linux folder issue
- Phase2A CSV package not needed because live DB validation works

If the live JDBC path works, keep using it for final viva preparation.
